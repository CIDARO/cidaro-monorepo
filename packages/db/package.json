{
  "name": "@cidaro/db",
  "version": "1.0.0",
  "description": "Database package of the CIDARO monorepo.",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "author": "Paolo Rollo",
  "license": "MIT",
  "devDependencies": {
    "@types/mongoose": "^5.7.27",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@types/node": "^14.0.14",
    "grpc": "^1.24.3",
    "mongoose": "^5.9.20"
  }
}
