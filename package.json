{
  "name": "@cidaro/monorepo",
  "private": false,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "postinstall": "npm run bootstrap",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "typescript": "^3.9.5"
  }
}
